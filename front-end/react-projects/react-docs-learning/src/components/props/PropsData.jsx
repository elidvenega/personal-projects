export default function PropsData({ name, language, country, age }) {
  return (
    <div>
      <p>
        Hello my {name} is and my age is {age} my language is {language} and
        where I'm from is the {country}.
      </p>
    </div>
  );
}
